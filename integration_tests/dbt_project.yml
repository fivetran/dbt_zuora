name: 'zuora_integration_tests'
version: '1.4.0'
profile: 'integration_tests'
config-version: 2

models:
  +persist_docs:
    relation: true
    columns: "{{ false if target.type in ('databricks') else true }}"
  +schema: "zuora_{{ var('directed_schema','dev') }}"

vars:
  zuora_schema: zuora_integration_tests
  zuora:
    zuora_account_identifier: "zuora_account_data"
    zuora_amendment_identifier: "zuora_amendment_data"
    zuora_contact_identifier: "zuora_contact_data"
    zuora_credit_balance_adjustment_identifier: "zuora_credit_balance_adjustment_data"
    zuora_invoice_identifier: "zuora_invoice_data"
    zuora_invoice_item_identifier: "zuora_invoice_item_data" 
    zuora_invoice_payment_identifier: "zuora_invoice_payment_data" 
    zuora_order_identifier: "zuora_order_data"
    zuora_payment_identifier: "zuora_payment_data"
    zuora_payment_method_identifier: "zuora_payment_method_data"
    zuora_product_identifier: "zuora_product_data"
    zuora_product_rate_plan_identifier: "zuora_product_rate_plan_data"
    zuora_product_rate_plan_charge_identifier: "zuora_product_rate_plan_charge_data"
    zuora_rate_plan_identifier: "zuora_rate_plan_data"
    zuora_rate_plan_charge_identifier: "zuora_rate_plan_charge_data"
    zuora_refund_identifier: "zuora_refund_data"
    zuora_refund_invoice_payment_identifier: "zuora_refund_invoice_payment_data"
    zuora_subscription_identifier: "zuora_subscription_data"
    zuora_taxation_item_identifier: "zuora_taxation_item_data"


dispatch:
  - macro_namespace: dbt_utils
    search_order: ['spark_utils', 'dbt_utils']

seeds:
  +docs:
    show: False
  +quote_columns: "{{ target.type == 'redshift' or var('fivetran_using_source_casing', false) }}"
  zuora_integration_tests:
    zuora_account_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        credit_balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        last_invoice_date: "timestamp"
        tax_exempt_effective_date: "timestamp"
        tax_exempt_expiration_date: "timestamp"
        total_debit_memo_balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        total_invoice_balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        unapplied_balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        unapplied_credit_memo_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        bill_cycle_day: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        mrr: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
    zuora_amendment_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        current_term: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        renewal_term: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
    zuora_contact_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
    zuora_credit_balance_adjustment_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_home_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        cancelled_on: "timestamp"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
    zuora_invoice_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        adjustment_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_without_tax: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_without_tax_home_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}" 
        credit_balance_adjustment_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        credit_memo_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        last_email_sent_date: "timestamp"
        payment_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        posted_date: "timestamp"
        amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_home_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        tax_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
    zuora_invoice_item_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        charge_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        charge_amount_home_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        charge_date: "timestamp"
        unit_price: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        quantity: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        tax_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
    zuora_invoice_payment_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_currency_rounding: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_home_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
    zuora_order_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
    zuora_payment_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        applied_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        applied_credit_balance_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"  
        settled_on: "timestamp"
        unapplied_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        amount_home_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        cancelled_on: "timestamp"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        submitted_on: "timestamp"
    zuora_payment_method_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        last_failed_sale_transaction_date: "timestamp"
        last_transaction_date_time: "timestamp"
        max_consecutive_payment_failures: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        num_consecutive_failures: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        total_number_of_error_payments: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        total_number_of_processed_payments: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        bank_identification_number: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        payment_retry_window: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        credit_card_country: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_aba_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_account_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_account_number_mask: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_account_type: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_address_1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_address_2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_bank_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_city: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_country: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_postal_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ach_state: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_branch_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_check_digit: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_city: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_postal_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_street_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_street_number: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_transfer_account_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_transfer_account_number_mask: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_transfer_account_type: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        bank_transfer_type: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        business_identification_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        city: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        company_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        country: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_address_1: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_address_2: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_city: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_expiration_month: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_expiration_year: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_holder_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_mask_number: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_postal_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_state: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        credit_card_type: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        device_session_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        email: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        existing_mandate: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        first_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        iban: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        identity_number: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        ipaddress: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        last_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandate_creation_date: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandate_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandate_reason: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandate_received: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandate_status: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        mandate_update_date: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        method_reference_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        method_specific_data: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        paypal_baid: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        paypal_email: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        paypal_preapproval_key: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        paypal_type: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        phone: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        postal_code: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        second_token_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        state: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        street_name: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        street_number: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        sub_type: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        token_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
        user_reference_id: "{{ 'string' if target.type in ['bigquery','spark','databricks'] else 'varchar' }}"
    zuora_product_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
    zuora_product_rate_plan_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
    zuora_product_rate_plan_charge_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        default_quantity: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        included_units: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        max_quantity: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        min_quantity: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}" 
        specific_billing_period:  "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        up_to_periods: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        bill_cycle_day: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
    zuora_rate_plan_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
    zuora_rate_plan_charge_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        booking_exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        dmrc: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        dmrchome_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        dtcv: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        dtcvhome_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}" 
        mrrhome_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        price_increase_percentage: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        tcv: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        tcvhome_currency: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        bill_cycle_day: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        mrr: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        quantity: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
    zuora_refund_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        refund_transaction_time: "timestamp"
        refund_date: "timestamp"
        cancelled_on: "timestamp"
        submitted_on: "timestamp"
    zuora_refund_invoice_payment_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
    zuora_subscription_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        contract_acceptance_date: "timestamp"
        contract_effective_date: "timestamp"
        initial_term: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}" 
        original_created_date: "timestamp"
        service_activation_date: "timestamp"
        subscription_end_date: "timestamp"
        subscription_start_date: "timestamp"
        term_end_date: "timestamp"
        term_start_date: "timestamp"
        version: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        cancelled_date: "timestamp"
        current_term: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
        renewal_term: "{{ 'INT64' if target.type == 'bigquery' else 'bigint' }}"
    zuora_taxation_item_data:
      +column_types:
        _fivetran_synced: "timestamp"
        created_date: "timestamp"
        updated_date: "timestamp"
        tax_date: "timestamp"
        balance: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        exchange_rate: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
        tax_amount: "{{ 'FLOAT64' if target.type == 'bigquery' else 'float' }}"
flags:
  send_anonymous_usage_stats: False
  use_colors: True
